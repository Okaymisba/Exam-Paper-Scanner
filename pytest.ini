[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Unit tests for individual functions and modules
    integration: Integration tests for Flask routes and database interactions
    e2e: End-to-end tests using a real browser via Playwright
addopts = -v --tb=short
